eating (tasty) ->
  hungry (ultrason < 10) ->
    hungry (ultrason < 6) ->
      drivethrough backward;
    stuffed ->
      drivethrough right;
    satisfied;
  stuffed ->
    drivethrough forward;
  satisfied;
enough;
